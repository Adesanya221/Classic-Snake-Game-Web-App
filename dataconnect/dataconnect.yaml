specVersion: "v1"
serviceId: "fisogame"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fisogame-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
